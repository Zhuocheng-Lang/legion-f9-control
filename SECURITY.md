# 安全策略（SECURITY.md）

> 非官方社区项目。Lenovo 和 Legion 是其各自权利人的商标；本项目与 Lenovo 无关联、无背书。

## 报告方式

**请勿公开提交安全 issue。** 通过 GitHub 私密漏洞报告（Security → Report a
vulnerability）私下报告。请在报告中说明：

- 影响的版本（`f9ctl --version` / `f9d --version`）；
- 平台与 transport（USB/BLE）；
- 复现步骤与**脱敏**日志（不含 MAC、序列号、主机名、用户名、绝对路径）。

## 支持版本

- `v1.0.0`（当前 release）：接收安全修复；
- 更早版本：不承诺修复。

## 响应时限目标

- 确认收到：48 小时内；
- 初步评估与影响分级：7 天内；
- 修复或缓解发布：30 天内（视严重程度可协商）。

## 范围

属于安全问题：

- 内存安全/panic（任意字节输入导致的崩溃——我们有属性测试，但欢迎补充）；
- 安全门绕过：raw 写白名单、OTA/report 5 收发、确认词/ACK 机制、
  `--force` 类参数绕过帧结构校验；
- IPC/socket 权限问题（0600 之外的暴露、跨用户访问）；
- 日志/JSON 输出泄露设备标识（MAC、序列号）。

按安全问题分流（但不属于代码缺陷）：

- **硬件损坏**：raw 写操作本身的物理风险；
- **协议未知行为**：未建模命令/字段引发的设备异常；
- **OTA 请求**：本项目永久不实现，任何 OTA 相关讨论先建立独立威胁模型。

## 协调

- 修复发布后，报告中提及的影响在 CHANGELOG 中披露（不含敏感细节）；
- 我们不会公开包含设备身份信息的日志；请勿在 issue 中贴出。
